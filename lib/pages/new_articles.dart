import 'package:flutter/material.dart';

class NewArticleList extends StatelessWidget {
  const NewArticleList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("New articles");
  }
}
